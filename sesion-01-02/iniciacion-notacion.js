// Notación

// ; Delimitar el final de una linea
const b = 4;

// . Se utiliza en los objetos para acceder en los atributos
// movil.anchura

// [] Para las listas, arreglos o arrays o para acceder aun valor dentro del mismo
const ar = [1, 2, 3, 4];
console.log(ar[2]);

// () Se utiliza para las funciones
function suma(a,b){
    // se escribe la función
}

// {} Llaves para objetos, funciones y estructuras de control
const movil = {
    anchura: 5,
    altura: 10
}

if (true){
    // todo lo q se encuentre aqui

    const constante2 = "hola";
}

