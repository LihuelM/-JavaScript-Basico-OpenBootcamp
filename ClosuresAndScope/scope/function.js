function greeting () {
    let userName = 'Ana';
    console.log(userName);

    if (userName === 'Ana') {
        console.log(`Hello ${userName}!`);
    }
} 

greeting();

// Variable local o fuction scope. Solo funciona dentro de la variable en la que fue asignada y declarada.